# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: testsuite

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

env:
  NODE_VERSION: 10.x

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('package-lock.json') }}
          restore-keys: npm-
      - run: npm ci
      - run: cp config.example.yml config.yml
      - run: npm run lint

  test_unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('package-lock.json') }}
          restore-keys: npm-
      - run: npm ci
      - run: cp config.example.yml config.yml
      - run: npm run test

  test_e2e:
    runs-on: ubuntu-latest
    env:
      SAUCE_MASTER_ACCESS_KEY: ${{ secrets.SAUCE_ACCESS_KEY }}
      SAUCE_MASTER_USERNAME: ${{ secrets.SAUCE_USERNAME }}
    services:
      db:
        image: postgres:9.5
      # api:
      #   image: sheltertechsf/askdarcel-api:latest
      #   credentials:
      #     username: ${{ secrets.DOCKER_USERNAME }}
      #     password: ${{ secrets.DOCKER_PASSWORD }}
      #   ports: ['3000:3000']
      #   options: --entrypoint "/sbin/my_init && bundle install"
      #   env:
      #     DATABASE_URL: 'postgres://postgres@db/askdarcel_development'
      #     TEST_DATABASE_URL: 'postgres://postgres@db/askdarcel_test'
      #     SECRET_KEY_BASE: notasecret
      #     # ALGOLIA_APPLICATION_ID: $ALGOLIA_APPLICATION_ID
      #     # ALGOLIA_API_KEY: $ALGOLIA_API_KEY
      #     # ALGOLIA_INDEX_PREFIX: $ALGOLIA_INDEX_PREFIX
      #     RAILS_ENV: development
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: ${{ env.NODE_VERSION }}
      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: npm-${{ hashFiles('package-lock.json') }}
          restore-keys: npm-
      - uses: docker/login-action@v1 
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - run: docker run -d --network askdarcel --name=api -p 3000:3000 sheltertechsf/askdarcel-api:latest bash -c 'bundle install --with=development && bundle exec rake db:setup db:populate && bundle exec rails server --binding=0.0.0.0'
      - run: npm ci
      - run: cp config.example.yml config.yml
      - run: TESTCAFE_RUNNING=true npm run dev &
      - uses: DevExpress/testcafe-action@latest
        with:
          skip-install: true
          args: 'chrome:headless testcafe/*.js -c 3 --skip-js-errors'
      # - run: ./travis-testcafe.sh